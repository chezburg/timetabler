# Copy this to .env and adjust if you want the backend container's file
# ownership to match a specific host user/group (matters most if you switch
# the backend's data volume to a bind mount in docker-compose.yml).
# Find your own with: id -u  /  id -g
PUID=1000
PGID=1000
